export const cache: RequestCache = "no-cache",
  cancel = true,
  configurable = true,
  deep = true,
  enumerable = true,
  html = true,
  immediate = true,
  mergeDefaults = true,
  once = true,
  persistent = true,
  second = 1000,
  writable = true;
