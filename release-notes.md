update project dependencies and integrate jsonrepair for manifest parsing

update various package versions including @ai-sdk/vue, @aws-sdk/client-s3,
@skaldapp packages, ai, electron, and add jsonrepair dependency

integrate jsonrepair to handle malformed JSON in manifest and index parsing
in App.vue and refactor data store to use removeHiddens helper function
